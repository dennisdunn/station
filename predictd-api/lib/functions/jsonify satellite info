// name: jsonify
// outputs: 1
const items = msg.payload.trim('\n').split('\n');
const props = Object.keys(msg.payload);

msg.payload = {};

props.forEach((prop,i)=>{
    const val = Number.parseFloat(items[i]);
    if(Number.isNaN(val)){
        msg.payload[prop]=items[i];
    }else{
        msg.payload[prop]=val;
    }
});

return msg;